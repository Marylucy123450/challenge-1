const calculate = () => {
    let speed = document.querySelector("#speed").value;

    let totalspeed =
        parseFloat(kis) ;


        if (speed <= 70 && percentage >= 80) {
            grades = "A";
        } else if (percentage <= 80 && percentage >= 60) {
            grades = "2";
        } else if (percentage <= 59 && percentage >= 40) {
            grades = "C";
        } else {
            grades = "F";
        }
        

};